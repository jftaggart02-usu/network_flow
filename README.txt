There is always an edge with capacity 0 going in the opposite direction of an edge with nonzero capacity
because the residual graph contains edges that go in the direction of flow and against the direction of flow.
The value of residual flow along an edge depends on if the edge goes in the direction of flow (nonzero capacity)
or against the direction of flow (zero capacity).
